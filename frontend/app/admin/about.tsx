import { AboutContent } from "@/src/components/AboutContent";

export default function AdminAboutScreen() {
  // App data (name, logo, colors, etc.) is now edited under App Settings.
  // This screen shows the info read-only.
  return <AboutContent />;
}
