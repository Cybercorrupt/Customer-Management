// Bundled default logo shown across the app (login, drawer, about, settings)
// whenever the admin has not uploaded a custom company logo.
export const DEFAULT_LOGO = require("@/assets/images/default-logo.png");
